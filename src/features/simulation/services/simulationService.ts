import type { DebtState, SimulationConfig, SimulationResult } from '../../../engine';

export const runSimulation = (_debts: DebtState[], _config: SimulationConfig): SimulationResult | null => {
	void _debts;
	void _config;
	return null;
};
